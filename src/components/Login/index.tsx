import { Button } from "../../assets/styles/UI";
import { LoginContainer } from "./styles";

function Login() {
  return (
    <LoginContainer>
      <Button>Seu negócio</Button>
      <Button>Entrar</Button>
    </LoginContainer>
  );
}

export default Login;
